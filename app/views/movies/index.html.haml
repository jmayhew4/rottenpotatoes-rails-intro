
%h1 All Movies

%table#movies
  %thead
    %tr
      %th{:class => @highlight_t}= link_to 'Movie Title', movies_path(param_sort: 'title')
      %th{:class => @highlight_r}= link_to 'Rating', movies_path(param_sort: 'rating')
      %th{:class => @highlight_rd}= link_to 'Release Date', movies_path(param_sort: 'release_date')
      %th More Info
  %tbody
    - @movies.each do |movie|
      %tr
        %td= movie.title 
        %td= movie.rating
        %td= movie.release_date
        %td= link_to "More about #{movie.title}", movie_path(movie)

= link_to 'Add new movie', new_movie_path
